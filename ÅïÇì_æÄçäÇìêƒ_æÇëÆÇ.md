# ПОДРОБНЫЙ ПЛАН СОЗДАНИЯ КОПИИ САЙТА SAMSON-BUKET.RU

## ЭТАП 1: АНАЛИЗ И ИССЛЕДОВАНИЕ ОРИГИНАЛЬНОГО САЙТА

### 1.1 Структурный анализ
- Изучить все страницы сайта (главная, каталог, контакты, о компании)
- Составить карту сайта (sitemap)
- Определить количество и типы страниц
- Записать структуру навигации

### 1.2 Технический анализ
- Открыть Chrome DevTools (F12)
- Проверить используемые технологии через вкладку Network
- Установить расширение Wappalyzer для определения стека
- Проанализировать HTML структуру (теги, классы, ID)
- Изучить CSS файлы (стили, медиа-запросы)
- Просмотреть JavaScript файлы и библиотеки

### 1.3 Дизайн анализ
- Сделать скриншоты всех страниц в полный рост
- Записать цветовую палитру (основные цвета, акценты)
- Определить используемые шрифты
- Измерить отступы и размеры элементов
- Проанализировать типографику (размеры заголовков, текста)

### 1.4 Функциональный анализ
- Протестировать все формы
- Проверить работу корзины
- Изучить фильтрацию товаров
- Проверить модальные окна
- Протестировать слайдеры и карусели
- Изучить анимации и переходы

### 1.5 Адаптивность
- Проверить сайт на разных разрешениях:
  - Desktop: 1920px, 1440px, 1200px
  - Tablet: 1024px, 768px
  - Mobile: 414px, 375px, 320px
- Записать breakpoints (точки изменения layout)

### 1.6 Сбор контента
- Скачать все изображения (логотип, товары, иконки, баннеры)
- Сохранить тексты всех страниц
- Записать все ссылки
- Сохранить видео (если есть)

---

## ЭТАП 2: ПРОЕКТИРОВАНИЕ И ПОДГОТОВКА

### 2.1 Выбор технологического стека

**Frontend:**
- HTML5 (семантическая разметка)
- CSS3 / SCSS (препроцессор)
- JavaScript (ES6+)
- jQuery (если требуется совместимость)

**CSS Framework (выбрать один):**
- Bootstrap 5 (быстрая разработка)
- Tailwind CSS (кастомизация)
- Чистый CSS (полный контроль)

**JavaScript библиотеки:**
- Swiper.js (слайдеры)
- AOS (анимации)
- jQuery Validation (формы)

**Backend (выбрать один):**
- Node.js + Express
- PHP (чистый или Laravel)
- Python + Flask/Django

**База данных:**
- MySQL
- PostgreSQL
- MongoDB

### 2.2 Структура проекта

Создать следующую файловую структуру:

```
samson-buket/
├── index.html
├── catalog.html
├── about.html
├── contacts.html
├── cart.html
├── assets/
│   ├── css/
│   │   ├── style.css
│   │   ├── responsive.css
│   │   └── animations.css
│   ├── js/
│   │   ├── main.js
│   │   ├── cart.js
│   │   ├── catalog.js
│   │   └── forms.js
│   ├── img/
│   │   ├── logo/
│   │   ├── products/
│   │   ├── icons/
│   │   └── banners/
│   └── fonts/
├── api/
│   ├── index.php (или server.js)
│   ├── products.php
│   ├── orders.php
│   └── contact.php
├── database/
│   └── schema.sql
├── admin/ (панель администратора)
├── package.json
├── .gitignore
└── README.md
```

### 2.3 Инициализация проекта

Выполнить команды:
```bash
mkdir samson-buket
cd samson-buket
git init
npm init -y
```

Установить зависимости:
```bash
npm install --save-dev sass webpack webpack-cli
npm install swiper aos jquery
```

---

## ЭТАП 3: РАЗРАБОТКА HTML-СТРУКТУРЫ

### 3.1 Создание базового шаблона (index.html)

**HEAD секция:**
```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Доставка цветов">
    <title>Букеты & цветы - Доставка цветов</title>
    <link rel="stylesheet" href="assets/css/style.css">
    <link rel="icon" href="assets/img/favicon.ico">
</head>
```

### 3.2 HEADER (Шапка сайта)

Элементы:
- Логотип (слева)
- Навигационное меню (центр)
- Телефон и часы работы (справа)
- Иконка корзины с счетчиком
- Кнопка мобильного меню (бургер)

Структура:
```html
<header class="header">
    <div class="container">
        <div class="header__logo">
            <img src="logo.png" alt="Букеты & цветы">
        </div>
        <nav class="header__nav">
            <ul>
                <li><a href="#catalog">Каталог</a></li>
                <li><a href="#about">О нас</a></li>
                <li><a href="#delivery">Доставка</a></li>
                <li><a href="#contacts">Контакты</a></li>
            </ul>
        </nav>
        <div class="header__contacts">
            <a href="tel:+79991234567">+7 (999) 123-45-67</a>
            <span>Работаем: 9:00 - 21:00</span>
        </div>
        <div class="header__cart">
            <span class="cart-count">0</span>
        </div>
    </div>
</header>
```

### 3.3 HERO секция (Главный баннер)

Элементы:
- Фоновое изображение
- Заголовок H1
- Подзаголовок
- Кнопка CTA (Call To Action)

### 3.4 КАТАЛОГ товаров

Структура карточки товара:
```html
<div class="product-card">
    <div class="product-card__image">
        <img src="bouquet.jpg" alt="Букет роз">
        <span class="product-card__badge">Хит</span>
    </div>
    <h3 class="product-card__title">Букет из 25 роз</h3>
    <p class="product-card__description">Красные розы премиум класса</p>
    <div class="product-card__price">2500 ₽</div>
    <button class="btn btn-primary">В корзину</button>
</div>
```

### 3.5 ПРЕИМУЩЕСТВА

Блок с иконками:
- Быстрая доставка
- Свежие цветы
- Гарантия качества
- Доступные цены

### 3.6 ОТЗЫВЫ

Карусель с отзывами клиентов:
- Фото клиента
- Имя
- Рейтинг (звезды)
- Текст отзыва

### 3.7 КОНТАКТЫ

- Форма обратной связи
- Адрес
- Телефон
- Email
- Карта (Яндекс.Карты)

### 3.8 FOOTER (Подвал)

- Меню навигации
- Социальные сети
- Копирайт
- Политика конфиденциальности

---

## ЭТАП 4: РАЗРАБОТКА CSS-СТИЛЕЙ

### 4.1 Подготовка

Создать файл `assets/css/style.css` со структурой:
```css
/* 1. Сброс стилей и переменные */
/* 2. Базовые стили */
/* 3. Компоненты */
/* 4. Секции */
/* 5. Адаптивность */
```

### 4.2 CSS переменные

```css
:root {
    --color-primary: #e91e63;
    --color-secondary: #2c3e50;
    --color-text: #333333;
    --color-bg: #ffffff;
    --font-main: 'Roboto', sans-serif;
    --font-heading: 'Montserrat', sans-serif;
    --transition: all 0.3s ease;
}
```

### 4.3 Сброс стилей

```css
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: var(--font-main);
    color: var(--color-text);
    line-height: 1.6;
}
```

### 4.4 Типографика

- H1: 48px, жирный
- H2: 36px, жирный
- H3: 24px, полужирный
- P: 16px, обычный

### 4.5 Кнопки

Создать стили для:
- Основная кнопка (.btn-primary)
- Второстепенная кнопка (.btn-secondary)
- Состояния: hover, active, disabled

### 4.6 Grid система

Использовать CSS Grid или Flexbox для layout:
```css
.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 15px;
}

.row {
    display: flex;
    flex-wrap: wrap;
    margin: 0 -15px;
}
```

### 4.7 Адаптивность

Создать медиа-запросы:
```css
/* Desktop */
@media (min-width: 1200px) { }

/* Tablet */
@media (max-width: 1024px) { }
@media (max-width: 768px) { }

/* Mobile */
@media (max-width: 576px) { }
```

### 4.8 Анимации

```css
@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

.fade-in {
    animation: fadeIn 0.5s ease;
}
```

---

## ЭТАП 5: JAVASCRIPT ФУНКЦИОНАЛЬНОСТЬ

### 5.1 Основной файл main.js

Структура:
```javascript
// 1. Константы и настройки
// 2. Утилиты
// 3. Инициализация
// 4. Обработчики событий
```

### 5.2 Мобильное меню

```javascript
const burger = document.querySelector('.burger');
const menu = document.querySelector('.header__nav');

burger.addEventListener('click', () => {
    menu.classList.toggle('active');
    burger.classList.toggle('active');
});
```

### 5.3 Слайдер (Swiper)

```javascript
const swiper = new Swiper('.swiper', {
    loop: true,
    autoplay: {
        delay: 5000,
    },
    pagination: {
        el: '.swiper-pagination',
        clickable: true,
    },
    navigation: {
        nextEl: '.swiper-button-next',
        prevEl: '.swiper-button-prev',
    },
});
```

### 5.4 Корзина (cart.js)

Функционал:
- Добавление товара
- Удаление товара
- Изменение количества
- Подсчет общей суммы
- Сохранение в localStorage

```javascript
class Cart {
    constructor() {
        this.items = JSON.parse(localStorage.getItem('cart')) || [];
    }

    addItem(product) {
        this.items.push(product);
        this.save();
        this.updateUI();
    }

    removeItem(productId) {
        this.items = this.items.filter(item => item.id !== productId);
        this.save();
        this.updateUI();
    }

    getTotal() {
        return this.items.reduce((sum, item) => sum + item.price, 0);
    }

    save() {
        localStorage.setItem('cart', JSON.stringify(this.items));
    }

    updateUI() {
        // Обновить счетчик корзины
        document.querySelector('.cart-count').textContent = this.items.length;
    }
}
```

### 5.5 Фильтрация каталога

```javascript
const filterBtns = document.querySelectorAll('.filter-btn');
const products = document.querySelectorAll('.product-card');

filterBtns.forEach(btn => {
    btn.addEventListener('click', () => {
        const category = btn.dataset.category;
        
        products.forEach(product => {
            if (category === 'all' || product.dataset.category === category) {
                product.style.display = 'block';
            } else {
                product.style.display = 'none';
            }
        });
    });
});
```

### 5.6 Валидация форм

```javascript
function validateForm(form) {
    const name = form.querySelector('[name="name"]').value;
    const phone = form.querySelector('[name="phone"]').value;
    const email = form.querySelector('[name="email"]').value;

    if (name.length < 2) {
        showError('Введите корректное имя');
        return false;
    }

    if (!validatePhone(phone)) {
        showError('Введите корректный телефон');
        return false;
    }

    if (!validateEmail(email)) {
        showError('Введите корректный email');
        return false;
    }

    return true;
}
```

### 5.7 Модальные окна

```javascript
function openModal(modalId) {
    const modal = document.getElementById(modalId);
    modal.classList.add('active');
    document.body.style.overflow = 'hidden';
}

function closeModal(modalId) {
    const modal = document.getElementById(modalId);
    modal.classList.remove('active');
    document.body.style.overflow = '';
}
```

### 5.8 Smooth Scroll

```javascript
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
        });
    });
});
```

### 5.9 Lazy Loading изображений

```javascript
const images = document.querySelectorAll('img[data-src]');

const imageObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            const img = entry.target;
            img.src = img.dataset.src;
            img.removeAttribute('data-src');
            imageObserver.unobserve(img);
        }
    });
});

images.forEach(img => imageObserver.observe(img));
```

---

## ЭТАП 6: РАЗРАБОТКА BACKEND И БАЗЫ ДАННЫХ

### 6.1 Структура базы данных

**Таблица: products (товары)**
```sql
CREATE TABLE products (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    price DECIMAL(10, 2) NOT NULL,
    image VARCHAR(255),
    category_id INT,
    in_stock BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Таблица: categories (категории)**
```sql
CREATE TABLE categories (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    slug VARCHAR(100) UNIQUE
);
```

**Таблица: orders (заказы)**
```sql
CREATE TABLE orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    customer_name VARCHAR(255) NOT NULL,
    customer_phone VARCHAR(20) NOT NULL,
    customer_email VARCHAR(255),
    delivery_address TEXT,
    total_amount DECIMAL(10, 2),
    status ENUM('new', 'processing', 'completed', 'cancelled') DEFAULT 'new',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Таблица: order_items (товары в заказе)**
```sql
CREATE TABLE order_items (
    id INT AUTO_INCREMENT PRIMARY KEY,
    order_id INT,
    product_id INT,
    quantity INT DEFAULT 1,
    price DECIMAL(10, 2),
    FOREIGN KEY (order_id) REFERENCES orders(id),
    FOREIGN KEY (product_id) REFERENCES products(id)
);
```

**Таблица: reviews (отзывы)**
```sql
CREATE TABLE reviews (
    id INT AUTO_INCREMENT PRIMARY KEY,
    customer_name VARCHAR(255),
    rating INT CHECK (rating >= 1 AND rating <= 5),
    comment TEXT,
    approved BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 6.2 API endpoints (PHP)

**api/products.php** - Получение товаров
```php
<?php
header('Content-Type: application/json');
include 'db.php';

$sql = "SELECT * FROM products WHERE in_stock = TRUE";
$result = $conn->query($sql);

$products = [];
while($row = $result->fetch_assoc()) {
    $products[] = $row;
}

echo json_encode($products);
?>
```

**api/orders.php** - Создание заказа
```php
<?php
header('Content-Type: application/json');
include 'db.php';

$data = json_decode(file_get_contents('php://input'), true);

$name = $conn->real_escape_string($data['name']);
$phone = $conn->real_escape_string($data['phone']);
$email = $conn->real_escape_string($data['email']);
$address = $conn->real_escape_string($data['address']);
$total = floatval($data['total']);

$sql = "INSERT INTO orders (customer_name, customer_phone, customer_email, delivery_address, total_amount) 
        VALUES ('$name', '$phone', '$email', '$address', $total)";

if ($conn->query($sql)) {
    $order_id = $conn->insert_id;
    
    // Отправить email уведомление
    mail('info@samson-buket.ru', 'Новый заказ #' . $order_id, 'Получен новый заказ');
    
    echo json_encode(['success' => true, 'order_id' => $order_id]);
} else {
    echo json_encode(['success' => false, 'error' => $conn->error]);
}
?>
```

**api/contact.php** - Форма обратной связи
```php
<?php
$name = $_POST['name'];
$email = $_POST['email'];
$message = $_POST['message'];

$to = 'info@samson-buket.ru';
$subject = 'Сообщение с сайта от ' . $name;
$body = "Имя: $name\nEmail: $email\n\nСообщение:\n$message";

if (mail($to, $subject, $body)) {
    echo json_encode(['success' => true]);
} else {
    echo json_encode(['success' => false]);
}
?>
```

### 6.3 Подключение к БД (db.php)

```php
<?php
$host = 'localhost';
$user = 'root';
$password = '';
$database = 'samson_buket';

$conn = new mysqli($host, $user, $password, $database);

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

$conn->set_charset("utf8");
?>
```

---

## ЭТАП 7: ИНТЕГРАЦИИ

### 7.1 Яндекс.Карты

```html
<div id="map" style="width: 100%; height: 400px;"></div>

<script src="https://api-maps.yandex.ru/2.1/?apikey=YOUR_KEY&lang=ru_RU"></script>
<script>
ymaps.ready(init);
function init() {
    var myMap = new ymaps.Map("map", {
        center: [55.76, 37.64],
        zoom: 15
    });
    
    var myPlacemark = new ymaps.Placemark([55.76, 37.64], {
        balloonContent: 'Букеты & цветы'
    });
    
    myMap.geoObjects.add(myPlacemark);
}
</script>
```

### 7.2 Маска для телефона

```javascript
// Использовать библиотеку IMask
const phoneMask = IMask(
    document.getElementById('phone'),
    {
        mask: '+{7}(000)000-00-00'
    }
);
```

### 7.3 reCAPTCHA (защита от спама)

```html
<div class="g-recaptcha" data-sitekey="YOUR_SITE_KEY"></div>
<script src="https://www.google.com/recaptcha/api.js"></script>
```

### 7.4 Счетчики (Яндекс.Метрика, Google Analytics)

```html
<!-- Яндекс.Метрика -->
<script type="text/javascript">
   (function(m,e,t,r,i,k,a){...})();
</script>

<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_ID"></script>
```

---

## ЭТАП 8: ОПТИМИЗАЦИЯ

### 8.1 Оптимизация изображений

- Конвертировать в WebP формат
- Сжать через TinyPNG или ImageOptim
- Использовать правильные размеры (не загружать 4K для превью)
- Добавить lazy loading

### 8.2 Минификация

```bash
# CSS
npm install -g clean-css-cli
cleancss -o style.min.css style.css

# JavaScript
npm install -g terser
terser main.js -o main.min.js -c -m
```

### 8.3 Кеширование (.htaccess)

```apache
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
</IfModule>
```

### 8.4 Сжатие Gzip

```apache
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript
</IfModule>
```

---

## ЭТАП 9: ТЕСТИРОВАНИЕ

### 9.1 Кросс-браузерное тестирование

Проверить в:
- Google Chrome
- Mozilla Firefox
- Safari
- Microsoft Edge
- Opera

### 9.2 Адаптивность

Протестировать на устройствах:
- Desktop (1920x1080, 1440x900, 1366x768)
- Tablet (iPad, Android планшеты)
- Mobile (iPhone, Android смартфоны)

### 9.3 Функциональное тестирование

Чек-лист:
- [ ] Все ссылки работают
- [ ] Формы отправляются
- [ ] Валидация работает
- [ ] Корзина добавляет/удаляет товары
- [ ] Слайдеры переключаются
- [ ] Модальные окна открываются/закрываются
- [ ] Фильтры работают
- [ ] Поиск работает
- [ ] Мобильное меню открывается

### 9.4 Производительность

Проверить через:
- Google PageSpeed Insights
- GTmetrix
- WebPageTest

Целевые показатели:
- Скорость загрузки < 3 сек
- First Contentful Paint < 1.8 сек
- Time to Interactive < 3.8 сек

### 9.5 SEO проверка

- [ ] Title теги на всех страницах
- [ ] Meta description заполнены
- [ ] H1 теги присутствуют
- [ ] Alt атрибуты у изображений
- [ ] Sitemap.xml создан
- [ ] Robots.txt настроен
- [ ] Микроразметка Schema.org

### 9.6 Безопасность

- [ ] SQL-инъекции защищены (prepared statements)
- [ ] XSS атаки предотвращены
- [ ] CSRF токены в формах
- [ ] Валидация на сервере
- [ ] SSL сертификат установлен

---

## ЭТАП 10: ДЕПЛОЙ И ЗАПУСК

### 10.1 Выбор хостинга

Варианты:
- **Shared hosting:** Beget, Timeweb, REG.RU (для начала)
- **VPS:** DigitalOcean, Vultr, Selectel (для роста)
- **Облако:** Yandex Cloud, AWS (для масштаба)

### 10.2 Регистрация домена

- Выбрать доменное имя
- Зарегистрировать через регистратора (REG.RU, Timeweb)
- Настроить DNS на хостинг

### 10.3 Настройка хостинга

1. Создать базу данных MySQL
2. Создать пользователя БД
3. Импортировать schema.sql
4. Настроить FTP доступ

### 10.4 Загрузка файлов

**Через FTP (FileZilla):**
1. Подключиться к серверу
2. Загрузить все файлы в папку public_html
3. Установить права доступа 755 для папок, 644 для файлов

**Через Git:**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin your-repo-url
git push -u origin main

# На сервере
git clone your-repo-url
```

### 10.5 Настройка SSL (HTTPS)

```bash
# Через Let's Encrypt (бесплатно)
# Обычно доступно в панели хостинга одним кликом
```

### 10.6 Настройка email

- Создать почтовый ящик info@your-domain.ru
- Настроить SMTP для отправки писем
- Проверить отправку заказов

### 10.7 Финальная настройка

**Создать sitemap.xml:**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
        <loc>https://your-domain.ru/</loc>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
    </url>
    <url>
        <loc>https://your-domain.ru/catalog.html</loc>
        <changefreq>daily</changefreq>
        <priority>0.8</priority>
    </url>
</urlset>
```

**Создать robots.txt:**
```
User-agent: *
Allow: /
Sitemap: https://your-domain.ru/sitemap.xml
```

### 10.8 Подключение аналитики

**Яндекс.Метрика:**
1. Создать счетчик на metrika.yandex.ru
2. Вставить код в <head>

**Google Analytics:**
1. Создать аккаунт на analytics.google.com
2. Получить ID отслеживания
3. Вставить код в <head>

### 10.9 Регистрация в поисковиках

**Яндекс.Вебмастер:**
1. Зарегистрироваться на webmaster.yandex.ru
2. Добавить сайт
3. Подтвердить права
4. Загрузить sitemap.xml

**Google Search Console:**
1. Зарегистрироваться на search.google.com
2. Добавить сайт
3. Подтвердить через DNS или файл
4. Отправить sitemap

---

## ЭТАП 11: ПОСЛЕ ЗАПУСКА

### 11.1 Мониторинг

- Проверять работоспособность ежедневно
- Отслеживать ошибки в логах
- Мониторить uptime через UptimeRobot

### 11.2 Резервное копирование

Настроить автоматический бэкап:
- Файлы сайта (раз в неделю)
- База данных (ежедневно)
- Хранить копии минимум 30 дней

### 11.3 Обновления

- Регулярно проверять безопасность
- Обновлять контент
- Добавлять новые товары
- Отвечать на отзывы

### 11.4 Маркетинг

- Настроить рекламу (Яндекс.Директ, Google Ads)
- Продвижение в соцсетях
- Email рассылка
- SEO оптимизация контента

---

## ЧЕКЛИСТ ФИНАЛЬНОЙ ПРОВЕРКИ

### Технические проверки:
- [ ] Сайт открывается по домену
- [ ] SSL сертификат работает (https://)
- [ ] Все страницы загружаются без ошибок
- [ ] Формы отправляются
- [ ] Email уведомления приходят
- [ ] База данных работает
- [ ] Корзина функционирует
- [ ] Мобильная версия корректна

### SEO проверки:
- [ ] Sitemap.xml загружен
- [ ] Robots.txt настроен
- [ ] Title и description на всех страницах
- [ ] H1-H6 структура соблюдена
- [ ] Alt теги у всех изображений
- [ ] Счетчики аналитики работают
- [ ] Open Graph теги добавлены

### Безопасность:
- [ ] SSL включен
- [ ] Формы защищены от спама
- [ ] Валидация данных работает
- [ ] Резервные копии настроены
- [ ] Права доступа к файлам правильные

### Производительность:
- [ ] Изображения оптимизированы
- [ ] CSS и JS минифицированы
- [ ] Кеширование включено
- [ ] Скорость загрузки < 3 сек
- [ ] PageSpeed Score > 80

---

## ПОЛЕЗНЫЕ РЕСУРСЫ

### Документация:
- MDN Web Docs: https://developer.mozilla.org
- W3C Validator: https://validator.w3.org
- Can I Use: https://caniuse.com

### Инструменты:
- Chrome DevTools
- VS Code + расширения (Live Server, Prettier)
- FileZilla (FTP клиент)
- phpMyAdmin (управление БД)
- Postman (тестирование API)

### Библиотеки и фреймворки:
- Swiper: https://swiperjs.com
- AOS: https://michalsnik.github.io/aos
- jQuery: https://jquery.com
- Bootstrap: https://getbootstrap.com

### Сервисы:
- TinyPNG (сжатие изображений)
- Google Fonts (шрифты)
- FontAwesome (иконки)
- Unsplash (бесплатные фото)

---

## ПРИМЕРНЫЕ СРОКИ

- **Этап 1-2:** 2-3 дня (анализ и подготовка)
- **Этап 3:** 3-4 дня (HTML разметка)
- **Этап 4:** 4-5 дней (CSS стили)
- **Этап 5:** 3-4 дня (JavaScript)
- **Этап 6:** 3-4 дня (Backend и БД)
- **Этап 7:** 2-3 дня (интеграции)
- **Этап 8:** 1-2 дня (оптимизация)
- **Этап 9:** 2-3 дня (тестирование)
- **Этап 10:** 1-2 дня (деплой)

**ИТОГО: 22-30 дней** (при работе 4-6 часов в день)

Для команды из 2-3 человек - 10-15 дней.

---

## ВАЖНЫЕ ЗАМЕЧАНИЯ

⚠️ **Авторские права:**
- Не копируйте дизайн один-в-один без разрешения
- Используйте собственные фотографии
- Напишите уникальные тексты

⚠️ **Юридические аспекты:**
- Создайте политику конфиденциальности
- Пользовательское соглашение
- Согласие на обработку персональных данных (GDPR, 152-ФЗ)

⚠️ **Безопасность:**
- Никогда не храните пароли в открытом виде
- Используйте prepared statements для SQL
- Валидируйте все данные на сервере

---

**Удачи в разработке! 🚀**


